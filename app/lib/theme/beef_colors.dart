import 'package:flutter/material.dart';

/// BEEF — original brand palette (NOT Grindr's trade dress). Our own steak-red,
/// sizzle, berry, lime, cream, char — plus the deeper `beefer` red. Values
/// mirror the landing site tokens and the chunk-1 scaffold.
abstract final class BeefColors {
  static const Color steak = Color(0xFFD62839);
  static const Color beefer = Color(0xFFA4161A);
  static const Color sizzle = Color(0xFFF77F00);
  static const Color berry = Color(0xFF7B2CBF);
  static const Color lime = Color(0xFFB7E318);
  static const Color cream = Color(0xFFFDF6E3);
  static const Color char = Color(0xFF1B1B1B);
}
